// TODO: when user visits site with extended link and don't have permission, we need to redirect on login page, but save link and redirect after it logs in

export const MainPage = () => {
  return <div>MainPage</div>;
};
